title: Nola Musicians for Palestine

collections:
  events:
  songs:
    output: true


defaults:
  - scope:
      path: ""
      type: "pages"
    values:
      layout: "default"

  - scope:
      path: ""
      type: "posts"
    values:
      layout: "post"

  - scope:
      type: "songs"
    values:
      layout: "song"
